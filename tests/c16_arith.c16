// Arithmetic and bit operations on 8 bit values.
// 7+5=12, 7-5=2, 7*5=35, 7/5=1, 7&5=5, 7|5=7, 7^5=2
int main() {
    int a = 7;
    int b = 5;
    out(0, a + b);
    out(1, a - b);
    out(2, a * b);
    out(3, a / b);
    out(4, a & b);
    out(5, a | b);
    out(6, a ^ b);
    return 0;
}
